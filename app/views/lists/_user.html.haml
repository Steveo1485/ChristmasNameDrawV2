%h3 Your List

- if @user.list.items.any?
  - @user.list.items.each do |item|
    .row
      .col-sm-12
        = item.name
        = link_to(raw("#{fa_icon("times-circle")} Delete Item"), item_path(item), method: :delete, "data-confirm" => "Are you sure you want to remove #{item.name} from your list?")

- else
  Nothing saved to your list yet. Add an item and get started!

.well
  %h6 Add Item To Your List
  = render "items/form"